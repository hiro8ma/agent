{
  "name": "agent-ts",
  "version": "0.0.1",
  "private": true,
  "description": "TypeScript + Bun のフレームワーク非依存エージェント実装",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint not configured yet'",
    "test": "bun test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0",
    "@google/genai": "^1.33.0",
    "openai": "^6.10.0",
    "zod": "^3.23.0",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
